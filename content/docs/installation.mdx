---
title: Installation
description: How to install and configure components in your project.
---

## Requirements

- Node.js 18.17 or Latest
- Next.js 13.4 or Latest
- Tailwind CSS

## Initialize Your Project

If you don't have a Next.js project yet, create one:

```bash
npx create-next-app@latest my-app --typescript --tailwind --app
cd my-app
```

## Install Dependencies

Skie uses the following dependencies:

```bash
npm install class-variance-authority clsx tailwind-merge
npm install -D @types/node
```


## Configure Path Aliases

Add the following to your `tsconfig.json`:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

## Add Utility Functions

Create a `lib/utils.ts` file:

```ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

## Install Components

Now you can start installing components. For example, to install the Raised Button:

```bash
package yet to be added
```


## Start Using Components

Import and use components in your app:

```tsx
go to components and copy the source code. It's Free to use! 
```

## Next Steps

- Browse the [component library](/docs/components/)
- File an Issue [here](https://github.com/ezDecode)
